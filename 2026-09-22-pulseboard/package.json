{
  "name": "pulseboard",
  "version": "0.1.0",
  "private": true,
  "description": "PulseBoard - a tiny self-hosted uptime monitor and status page MVP (no external DB, no paid APIs).",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
