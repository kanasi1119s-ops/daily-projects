# 公開手順書（人間が実行）

このツールは静的サイト（HTML/CSS/JSのみ、ビルド不要）です。バックエンドやデータベースは存在しません。

## ビルド確認（実施済み）

- 依存パッケージなし。`index.html` を直接開く、または任意の静的サーバーで配信するだけで動作します。
- ローカル動作確認は Playwright で実施済み（QA参照）。

## 公開先候補（いずれも無料枠で開始可能）

1. **GitHub Pages**
   - リポジトリの `Settings > Pages` で公開ブランチとフォルダ（`2026-09-23-invoice-generator/`）を指定
   - 独自ドメインを使う場合は `CNAME` ファイルを追加（★ドメイン取得・DNS設定は人間が実施）

2. **Netlify / Vercel**
   - リポジトリを接続し、Build command を空、Publish directory を `2026-09-23-invoice-generator` に設定するだけでデプロイ可能

## 人間が実施する項目（このセッションでは実行しない）

- [ ] ドメイン取得・DNS本番反映
- [ ] 上記いずれかのホスティングサービスへの実際のデプロイ操作
- [ ] 独自ドメイン・SNSでの一般公開・告知の実施
- [ ] （将来）決済導入時：Stripe等アカウントの本番接続
- [ ] （将来）プライバシーポリシーページの追加とリンク設置

## 今後のCI案

現状はビルドステップがないため自動CIは必須ではないが、HTMLの構文チェック（`htmlhint`等）をGitHub Actionsに追加することを次サイクルで検討。
